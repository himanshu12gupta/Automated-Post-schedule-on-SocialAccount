<style>

input[type="text"],
input[type="email"],
input[type="password"],
textarea,
select {
  width: 100%; /* Make fields take full width of the container */
  padding: 10px;
  font-size: 1em;
  border: 1px solid #ccc; /* Light border for fields */
  border-radius: 5px;
  box-sizing: border-box; /* Include padding and border in element's total width and height */
  margin-bottom: 20px; /* Space below each field */
}

input[type="text"]:focus,
input[type="email"]:focus,
input[type="password"]:focus,
textarea:focus,
select:focus {
  border-color: #1abc9c; /* Change border color on focus to match primary button */
  outline: none; /* Remove default outline */
}

/* Add styles for form labels */
label {
  display: block;
  margin-bottom: 5px;
  font-weight: bold;
  color: #fff; /* Darker color for readability */
}

/* Optional: add spacing around form elements */
.form-group {
  margin-bottom: 20px; /* Space between form elements */
}

h1 {
  font-size: 2.5em;
  color: #fff;
  margin: 100px 0 30px 0; /* Added margin at the top */
  text-align: center;
}


.container {
  max-width: 800px;
  margin: 0 auto;
  padding: 30px;
}

.btn {
  padding: 10px 20px;
  font-size: 1em;
  border-radius: 5px;
  text-transform: uppercase;
  font-weight: bold;
  transition: background-color 0.3s ease;
}

.btn-primary {
  background-color: #1abc9c;
  color: #fff;
}

.btn-primary:hover {
  background-color: #16a085;
}

.btn-secondary {
  background-color: #95a5a6;
  color: #fff;
}

.btn-secondary:hover {
  background-color: #7f8c8d;
}

.link-group {
  margin-top: 20px;
  text-align: center;
}

.link-group a {
  color: #3498db;
  font-weight: bold;
  text-decoration: none;
}

.link-group a:hover {
  text-decoration: underline;
}

</style>
<div class="container">
  <h1>Editing Social Account</h1>

  <%= render "form", social_account: @social_account %>

  <div class="link-group">
    <%= link_to "Show this social account", @social_account, class: "btn btn-primary" %> |
    <%= link_to "Back to social accounts", social_accounts_path, class: "btn btn-secondary" %>
  </div>
</div>
